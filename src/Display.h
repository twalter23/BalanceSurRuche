#ifndef _DISPLAY_H    // Put these two lines at the top of your file.
#define _DISPLAY_H    // (Use a suitable name, usually based on the file name.)

bool EnableOrNotDisplay(void);
void SetDisplayOff(void);
void SetDisplayOn(void);

#endif // _HEADERFILE_H    // Put this line at the end of your file. (to avoid multiple define issue)